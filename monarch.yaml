vars:
  user: "melih"
  alias_name: "ll"
  managed_by: "Monarch 🏰"

resources:
  - id: "bashrc_config"
    type: file
    name: "bashrc-custom"
    path: "./test_home/.bashrc"
    content: |
      # {{ .managed_by }}
      # Welcome, {{ .user }}!
      alias {{ .alias_name }}='ls -la'

resources1:
  - name: "my-web-app"
    type: "container"
    image: "nginx:latest"
    state: "running"
    ports:
      - "8080:80"
