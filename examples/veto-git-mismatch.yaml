resources:
  - id: git-mismatch
    type: git
    parameters:
      # We will point to a folder that has Hello-World (setup in script)
      # But we claim it should be Veto repo.
      repo: https://github.com/melih-ucgun/veto.git
      dest: /tmp/veto-git-verify/mismatch
